sudo apt update
sudo apt install python3-tk python3-venv -y
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt || sudo apt install flask (maybe its already installed in your kali linex machine)

sudo python vrog_dashboard.py || sudo python3 vrog.py
